{
  "type": "module",
  "dependencies": {
    "csv-parser": "^3.2.0",
    "csv-writer": "^1.6.0",
    "express": "^4.17.1",
    "node-schedule": "^2.1.1",
    "nodemailer": "^6.9.16",
    "open": "^8.4.0",
    "socket.io": "^4.8.1"
  },
  "scripts": {
    "prestart": "npm run install-all",
    "start": "node main.js",
    "install-all": "npm install && cd web1 && npm install && cd ../web2 && npm install"
  }
}
